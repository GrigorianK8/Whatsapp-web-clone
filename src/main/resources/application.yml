spring:
    application:
        name: Whatsapp-web-clone
    datasource:
        url: jdbc:mysql://localhost:3306/whatsapp
        username: root
        password: root
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
